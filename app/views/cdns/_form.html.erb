<%= form_for(@cdn) do |f| %>
  <% if @cdn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cdn.errors.count, "error") %> prohibited this cdn from being saved:</h2>

      <ul>
      <% @cdn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dns %><br>
    <%= f.text_field :dns %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
