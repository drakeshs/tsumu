<%= form_for(@database_group) do |f| %>
  <% if @database_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@database_group.errors.count, "error") %> prohibited this database_group from being saved:</h2>

      <ul>
      <% @database_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :port %><br>
    <%= f.text_field :port %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
