<%= form_for(@cache_group) do |f| %>
  <% if @cache_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cache_group.errors.count, "error") %> prohibited this cache_group from being saved:</h2>

      <ul>
      <% @cache_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :port %><br>
    <%= f.text_field :port %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
