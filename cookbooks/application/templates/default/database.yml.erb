# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  encoding: utf8
  username: root
  password:
  host: db.sandbox.com
  port: 3306
  database: sandbox

development:
  <<: *default

test:
  <<: *default

staging:
  <<: *default

production:
  <<: *default
